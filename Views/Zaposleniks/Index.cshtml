@model IEnumerable<getfitEF.Models.Zaposlenik>

@{
    ViewBag.Title = "Index";
}
@{
    Layout = "~/Views/Shared/ViewPage1.cshtml";
}
<br />
<br />
<br />
<h2>Zaposlenici</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Spol
        </th>
        <th>
            Ime
        </th>
        <th>
            Prezime
        </th>
        <th>
            Datum rođenja
        </th>
        <th>
            Broj kartice
        </th>
        <th>
            Broj lične karte
        </th>
        <th>
            Adresa
        </th>
        <th>
            Telefon
        </th>
        <th>
            E-mail
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SifSpol.Naziv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prezime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DatumRodjenja)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrojKartice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrojLicneKarte)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdresaPrebivalista)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
